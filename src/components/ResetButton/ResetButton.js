import React from "react";
import Button from "../Button/Button";

function ResetButton({ type, children }) {
  return <Button type={"button"} children="reset"></Button>;
}

export default ResetButton;
